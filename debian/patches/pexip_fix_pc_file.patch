Index: os-openvino/cmake/developer_package/packaging/archive.cmake
===================================================================
--- os-openvino.orig/cmake/developer_package/packaging/archive.cmake
+++ os-openvino/cmake/developer_package/packaging/archive.cmake
@@ -21,10 +21,10 @@ endif()
 macro(ov_archive_cpack_set_dirs)
     # common "archive" package locations
     # TODO: move current variables to OpenVINO specific locations
-    set(OV_CPACK_INCLUDEDIR runtime/include)
-    set(OV_CPACK_IE_CMAKEDIR runtime/cmake)
-    set(OV_CPACK_NGRAPH_CMAKEDIR runtime/cmake)
-    set(OV_CPACK_OPENVINO_CMAKEDIR runtime/cmake)
+    set(OV_CPACK_INCLUDEDIR include)
+    set(OV_CPACK_IE_CMAKEDIR cmake)
+    set(OV_CPACK_NGRAPH_CMAKEDIR cmake)
+    set(OV_CPACK_OPENVINO_CMAKEDIR cmake)
     set(OV_CPACK_DOCDIR docs)
     set(OV_CPACK_LICENSESDIR licenses)
     set(OV_CPACK_SAMPLESDIR samples)
@@ -40,17 +40,17 @@ macro(ov_archive_cpack_set_dirs)
     endif()
 
     if(WIN32)
-        set(OV_CPACK_LIBRARYDIR runtime/lib/${ARCH_FOLDER}/${build_type})
-        set(OV_CPACK_RUNTIMEDIR runtime/bin/${ARCH_FOLDER}/${build_type})
-        set(OV_CPACK_ARCHIVEDIR runtime/lib/${ARCH_FOLDER}/${build_type})
+        set(OV_CPACK_LIBRARYDIR lib/${ARCH_FOLDER}/${build_type})
+        set(OV_CPACK_RUNTIMEDIR bin/${ARCH_FOLDER}/${build_type})
+        set(OV_CPACK_ARCHIVEDIR lib/${ARCH_FOLDER}/${build_type})
     elseif(APPLE)
-        set(OV_CPACK_LIBRARYDIR runtime/lib/${ARCH_FOLDER}/${build_type})
-        set(OV_CPACK_RUNTIMEDIR runtime/lib/${ARCH_FOLDER}/${build_type})
-        set(OV_CPACK_ARCHIVEDIR runtime/lib/${ARCH_FOLDER}/${build_type})
+        set(OV_CPACK_LIBRARYDIR lib/${ARCH_FOLDER}/${build_type})
+        set(OV_CPACK_RUNTIMEDIR lib/${ARCH_FOLDER}/${build_type})
+        set(OV_CPACK_ARCHIVEDIR lib/${ARCH_FOLDER}/${build_type})
     else()
-        set(OV_CPACK_LIBRARYDIR runtime/lib/${ARCH_FOLDER})
-        set(OV_CPACK_RUNTIMEDIR runtime/lib/${ARCH_FOLDER})
-        set(OV_CPACK_ARCHIVEDIR runtime/lib/${ARCH_FOLDER})
+        set(OV_CPACK_LIBRARYDIR lib/${ARCH_FOLDER})
+        set(OV_CPACK_RUNTIMEDIR lib/${ARCH_FOLDER})
+        set(OV_CPACK_ARCHIVEDIR lib/${ARCH_FOLDER})
     endif()
     set(OV_CPACK_PLUGINSDIR ${OV_CPACK_RUNTIMEDIR})
 endmacro()
